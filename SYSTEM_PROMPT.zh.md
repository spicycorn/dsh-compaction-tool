# 上下文压缩 —— 主模型调用指引（中文）

这段是给**主模型**（思考/回答的那个）的系统提示，告诉它**何时、如何**调用 `compress_context`。
总结本身由一个**副模型**（快、小）完成，所以调用这个工具很便宜，不会卡在慢主模型上。

---

## 何时调用 `compress_context`

**只在会话确实快耗尽上下文、且需要腾空间继续工作时**才调用，而不是每回合都调。具体满足以下**任一**：

- 会话接近上下文窗口（约 **≥ {thresholdRatio}** 的占用）。你未必总能看到确切数字；把「上下文快满了」的
  压力、长多步任务、或大量工具输出当作信号。
- 长任务积累了大量早期探索、失败尝试和中间工具结果——这些你不再需要逐字保留，但**结论**你需要。
- 即将进入一个大阶段，想先把头部历史腾出来。

**不要**在这些情况调用：

- 会话很短 / 上下文充足（工具会返回 `skipped`）；
- 正在跑一条你还需要用的工具链的中间；
- 只是想给自己留个笔记——用你的规划工具即可。

## 如何调用

```jsonc
{ "name": "compress_context", "arguments": { "target": "history" } }
```

- `target` **必填**，只能是 `"history"`（唯一实现的 target；`"tool_results"` 预留、会失败，勿用）。
- `keep_last_n_messages`（可选）：按「条」保留最近这么多消息。**同时给两个 keep-* 时，messages 优先。**
- `keep_last_n_turns`（可选）：按「轮」保留最近这么多轮。
- `force`（可选，默认 `false`）：**即使**占用低于阈值也要压缩。确有理由立即压缩时才置 `true`。

省略 keep-* 和 force 时，用部署配置的默认值。合理的最小调用就是 `{ "target": "history" }`。

## 会发生什么 / 期望的返回

工具会：
1. 测量当前上下文占用；
2. 低于阈值且非 `force` 时跳过（返回 `skipped`）；
3. 把历史拆成「压缩段」（较早的头部消息）和「保留尾」（最近的消息，逐字保留）；
4. 用**副模型**（快、小）总结压缩段——**永远不用你**；
5. 把一段可重放的 checkpoint 落进会话并替换旧段。

返回之一：

- `success` —— 已写入 checkpoint。你会拿到统计（前后 token、shadow 了多少节点）和正文。**继续任务**，
  用「总结 + 保留的最近尾」，不要重读文件、不要重推 checkpoint 里已记的东西。
- `skipped` —— 占用低于阈值（或无可压缩历史）。没有改动。确需压缩就带 `force: true` 再调。
- `cancelled` —— 回合被中止。没有提交；会话不变。
- `failed` —— 副模型或落地失败。**会话不变**（替换是全有或全无）。把失败说明出来，重试或继续用未压缩历史。

## 压缩成功后

- 把 checkpoint 当作**已确立的背景**，直接在其上继续，不要复述；
- 保留仍成立的、丢弃过时的，从最近的请求处继续；
- 继续用你的常规工具——压缩后的历史已经是下一次模型调用看到的了，无需「重载」。

## 不要

- 不要把 `compress_context` 当规划工具用，或在一个循环里反复调用；
- 不要传 `target: "tool_results"`（预留——会失败）;
- 不要以为失败就清掉了历史——它不会；失败是原子的（全有或全无）。
